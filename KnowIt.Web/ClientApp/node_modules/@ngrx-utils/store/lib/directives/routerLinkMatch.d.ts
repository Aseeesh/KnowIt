import type { QueryList, AfterContentInit, ElementRef, OnChanges, OnDestroy, Renderer2 } from '@angular/core';
import { Router, RouterLink, RouterLinkWithHref } from '@angular/router';
export interface MatchExp {
    [classes: string]: string;
}
/**
 * This directive will give you ability to add a class to the element
 * when router url match a regular expression.
 * The syntax is same with ngClass but replace the true/false expression
 * with your string based regexp (like the string you pass to new RegExp(''))
 *
 * @example
 * Example: active-class will be added to a tag when router URL
 * contains this segment: products/12345
 *
 * ```html
 * <a routerLink="/products"
 *  [routerLinkMatch]="{
 *    "active-class": "products/\\d+"
 *  }"></a>
 * ```
 */
export declare class RouterLinkMatch implements OnDestroy, OnChanges, AfterContentInit {
    private router;
    private _renderer;
    private _ngEl;
    private _curRoute;
    private _matchExp;
    links: QueryList<RouterLink>;
    linksWithHrefs: QueryList<RouterLinkWithHref>;
    set routerLinkMatch(matchExp: MatchExp);
    constructor(router: Router, _renderer: Renderer2, _ngEl: ElementRef);
    ngOnChanges(): void;
    ngAfterContentInit(): void;
    private _update;
    private _toggleClass;
    ngOnDestroy(): void;
}
export declare class RouterLinkMatchModule {
}
